// TODO create the full macro field, based on reference

export const macroField = {
    name: "base/macro",
    type: "leaf",
    renderAsCard: (ctx) => `TODO: make macro ${JSON.stringify(ctx.schema)}`
}